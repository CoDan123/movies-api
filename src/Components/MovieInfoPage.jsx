import react from "react";

const MovieInfoPage = (props) => {
    return(
        <div className="">
            <h1>{props.movieInfoPage.title}</h1>
        </div>
    )
}

export default MovieInfoPage;

